apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: graphql-service
  namespace: qp
spec:
  rules:
  - host: ||host||
    http:
      paths:
      - backend:
          serviceName: graphql-service
          servicePort: 80